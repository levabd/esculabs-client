<Page
    x:Class="Client.Pages.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Client.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid HorizontalAlignment="Center" Height="Auto" Width="Auto" VerticalAlignment="Center" Margin="0,0,0,160">
            <TextBlock HorizontalAlignment="Center"  TextWrapping="Wrap" Text="Вход в систему" Style="{ThemeResource HeaderTextBlockStyle}"  VerticalAlignment="Top"/>
            <TextBox x:Name="LoginTextBox" PlaceholderText="Имя пользователя" HorizontalAlignment="Center" Margin="0,93,0,0" TextWrapping="NoWrap" VerticalAlignment="Top" Width="365" Height="25" Text="Antrille"/>
            <PasswordBox x:Name="PasswordBox" PlaceholderText="Пароль" HorizontalAlignment="Center" Margin="0,160,0,0" VerticalAlignment="Top" Width="365" Height="25" KeyUp="PasswordBox_KeyUp" Password="superadmin1!"/>
            <Button x:Name="LoginButton" Content="Войти" HorizontalAlignment="Center" Margin="0,230,0,0" VerticalAlignment="Top" Width="126" Click="LoginButton_Click" Height="40"/>
        </Grid>
    </Grid>
</Page>
